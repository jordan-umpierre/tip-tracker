// Routes only map URLs to screens. Screen composition stays under src/ so a
// navigation change does not move application code with it.
export { default } from '../../src/screens/logShift/DetailsStepScreen';
