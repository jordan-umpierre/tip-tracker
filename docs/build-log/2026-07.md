# Build log — July 2026

Archived out of `BUILD_LOG.md` on 2026-07-30, when that file passed the ~500
line split threshold. Split by calendar month rather than by number, same
rule as `docs/brainstorm/`'s Q&A archive.

This single file then passed ~500 lines itself, the same day, so it split
again — this time by phase, same rule as the first split. Every commit from
July lives in one of these five:

- [2026-07-docs-and-process.md](2026-07-docs-and-process.md) — the initial
  commit through the cold-agent handoff system: product docs, the decision
  log split, `check-docs.sh` and its own real bugs, `test-schema.sh`, the
  README rewrite, the Q&A archive's first split.
- [2026-07-app-and-sqlite.md](2026-07-app-and-sqlite.md) — scaffolding the
  Expo app, adding this build log, wiring `schema.sql` into `expo-sqlite`.
- [2026-07-data-layer.md](2026-07-data-layer.md) — `jobs.ts` and
  `shifts.ts`, the data-access functions a screen actually calls.
- [2026-07-log-shift-screen.md](2026-07-log-shift-screen.md) — the first
  hand-written React Native UI: create, list, delete, and edit a shift.
- [2026-07-gross-totals.md](2026-07-gross-totals.md) — the totals row, the
  first pure-calculation module and its test, the money rounding rule (D5),
  and the shared formatters. Completes MVP Layer 0.

Companion docs: `../../BUILD_LOG.md` for the top-level index, `../../DECISIONS.md`
for the numbered decisions, `../../BRAINSTORM.md` for product thinking and
the Order of Operations.
